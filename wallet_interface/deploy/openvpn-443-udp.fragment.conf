# OpenVPN can share numeric port 443 with HTTPS only when it uses UDP.
# Host nginx owns TCP 443 for https://211-ai.com; OpenVPN owns UDP 443.
port 443
proto udp

# Keep the rest of the server configuration from the existing deployment:
# dev, ca, cert, key, dh, server/pool, push routes, cipher/auth, tls-crypt,
# user/group, persist-key, persist-tun, status, logs, and management settings.
